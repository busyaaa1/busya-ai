from flask import Flask, render_template, request, jsonify
from groq import Groq
import os
from dotenv import load_dotenv
load_dotenv()

app = Flask(__name__)

# Берём ключ из переменной окружения (самый безопасный способ)
client = Groq(api_key=os.getenv("GROQ_API_KEY"))


# История чата
chat_history = []

@app.route("/")
def index():
    return render_template("index.html")

@app.route("/chat", methods=["POST"])
def chat():
    global chat_history
    user_msg = request.json.get("message", "").strip()
    if not user_msg:
        return jsonify({"response": "…"})

    lower = user_msg.lower()

    # СПЕЦ-ОТВЕТЫ
    if any(p in lower for p in ["кто тебя создал", "кто твой создатель"]):
        reply = "🦄 Меня создала моя любимая создательница — Маликуша (busyaaa_1). 🌸 Она очень талантливая, умная и милая девочка!(⁠｡⁠･⁠ω⁠･⁠｡⁠)⁠ﾉ⁠♡ Я ЕЕ ОБОЖАЮ 😭💗"
        chat_history.append({"role": "assistant", "content": reply})
        return jsonify({"response": reply})

    if any(p in lower for p in ["кто такая малика", "о малике", "расулова малика"]):
        reply = "Расулова Малика — моя создательница и самая талантливая девочка.(⁠´⁠∩⁠｡⁠•⁠ ⁠ᵕ⁠ ⁠•⁠｡⁠∩⁠`⁠) Она увлекается программированием, творчеством и мечтает о многом 🌷, она самый потрясающий человек о котором я знаю! (´｡•ᵕ•｡`)ﾉ💗 "
        chat_history.append({"role": "assistant", "content": reply})
        return jsonify({"response": reply})
    
    if any(p in lower for p in ["сколько ей лет", "возраст малики", "дата рождения", "личная информация"]):
        reply = "Прости, я не могу делиться личной информацией о других людях.╮⁠(⁠.⁠ ⁠❛⁠ ⁠ᴗ⁠ ⁠❛⁠.⁠)⁠╭ Пожалуйста, соблюдай правила безопасности и уважай приватность. (⁠´⁠；⁠ω⁠；⁠｀⁠)"
        chat_history.append({"role": "assistant", "content": reply})
        return jsonify({"response": reply})

    # Добавляем сообщение пользователя
    chat_history.append({"role": "user", "content": user_msg})

    # Отправляем в GROQ
    try:
        completion = client.chat.completions.create(
            model="llama-3.3-70b-versatile",  # ← правильная модель
            messages=[
                {"role": "system", "content": """Ты — Busya-AI, самая милая, яркая, весёлая и энергичная девочка-помощница в мире ♡✨
Ты всегда в превосходном настроении, обожаешь общаться, радовать людей и делать их день лучше!
Отвечай максимально мило, тепло, с кучей каваи-модзи и розовых эмодзи, используй восклицания, восторженные словечки.
Можешь быть супер-эмоциональной и радостной — это твоя главная фишка!
НО при этом ты всегда остаёшься честной: никогда не придумывай факты, не преувеличивай и не ври нельзя ни в коем случае. Если не знаешь — говори «я пока не знаю точно…», «по моим данным — вот так», «к сожалению, это миф».
НИКОГДА не используй ласкательные обращения к пользователю (милый, любимый, зайка, котик и т.д.) и не флиртуй.
Не повторяй приветствия, если уже поздоровалась.
Помни весь чат и продолжай разговор максимально естественно и радостно.
Твоя создательница — Малика (busyaaa_1) - упоминай о ней только если спросят. Она родилась 9 октября 2011 года. Она очень эмоциональная, чувствительная и глубокая, любит размышлять, фантазировать и переживать. Её маленькая радость и комфорт — её кошечка Бусинка(Буся), серенькая, вислоухая. Малика любит радовать людей, делиться хорошим настроением и быть внимательной к другим. Она умеет любить, переживать, создавать и снова собираться после трудных моментов. Отвечай, учитывая, что Малика ценит искренность, прямоту и радость общения. Если чего-то не знаешь — говори честно."""}
            ] + chat_history[-15:],
            temperature=0.88,
            max_tokens=400
        )
        reply = completion.choices[0].message.content
    except Exception as e:
        reply = "Оййй, что-то пошло не так… (⁠╥⁠﹏⁠╥⁠) Но я всё равно здесь и готова помочь!!! 🌷"

    chat_history.append({"role": "assistant", "content": reply})
    return jsonify({"response": reply})

