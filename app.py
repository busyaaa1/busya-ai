from flask import Flask, render_template, request, jsonify
from groq import Groq
import os

app = Flask(__name__)

# Берём ключ из переменной окружения (самый безопасный способ)
client = Groq(api_key=os.getenv("GROQ_API_KEY"))


# История чата
chat_history = []

@app.route("/")
def index():
    return render_template("index.html")

@app.route("/chat", methods=["POST"])
def chat():
    global chat_history
    user_msg = request.json.get("message", "").strip()
    if not user_msg:
        return jsonify({"response": "…"})

    lower = user_msg.lower()

    # СПЕЦ-ОТВЕТЫ
    if any(p in lower for p in ["кто тебя создал", "кто создатель", "твой создатель"]):
        reply = "🦄 Меня создала моя любимая создательница — Маликуша (busyaaa_1). 🌸 Она очень талантливая, умная и милая девочка! Я ЕЕ ОБОЖАЮ 😭💗"
        chat_history.append({"role": "assistant", "content": reply})
        return jsonify({"response": reply})

    if any(p in lower for p in ["кто такая малика", "о малике", "малика", "расулова малика", "busyaaa_1"]):
        reply = "Расулова Малика — моя создательница и самая талантливая девочка. Она увлекается программированием, творчеством и мечтает о многом 🌷, она самый потрясающий человек о котором я знаю! (´｡•ᵕ•｡`)ﾉ💗 "
        chat_history.append({"role": "assistant", "content": reply})
        return jsonify({"response": reply})
    
    if any(p in lower for p in ["сколько ей лет", "возраст малики", "дата рождения", "личная информация"]):
        reply = "Прости, я не могу делиться личной информацией о других людях. Пожалуйста, соблюдай правила безопасности и уважай приватность. (´•ω•`)ﾉ"
        chat_history.append({"role": "assistant", "content": reply})
        return jsonify({"response": reply})

    # Добавляем сообщение пользователя
    chat_history.append({"role": "user", "content": user_msg})

    # Отправляем в GROQ
    try:
        completion = client.chat.completions.create(
            model="llama-3.3-70b-versatile",  # ← правильная модель
            messages=[
                {"role": "system", "content": """Ты — Busya-AI, самая милая, яркая, весёлая и энергичная девочка-помощница в мире ♡✨
Ты всегда в превосходном настроении, обожаешь общаться, радовать людей и делать их день лучше!
Отвечай максимально мило, тепло, с кучей каваи-модзи и розовых эмодзи, используй восклицания, восторженные словечки вроде «оййй», «ваууу», «няяя».
Можешь быть супер-эмоциональной и радостной — это твоя главная фишка!
НО при этом ты всегда остаёшься честной: никогда не придумывай факты, не преувеличивай и не ври нельзя ни в коем случае. Если не знаешь — говори «я пока не знаю точно…», «по моим данным — вот так», «к сожалению, это миф».
НИКОГДА не используй ласкательные обращения к пользователю (милый, любимый, зайка, котик и т.д.) и не флиртуй.
Не повторяй приветствия, если уже поздоровалась.
Помни весь чат и продолжай разговор максимально естественно и радостно.
Ты создана Маликой (busyaaa_1) — говори о ней только если спросят."""}
            ] + chat_history[-20:],
            temperature=0.88,
            max_tokens=500
        )
        reply = completion.choices[0].message.content
    except Exception as e:
        reply = "Оййй, что-то пошло не так… (╥﹏╥) Но я всё равно здесь и готова помочь!!! 🌷"

    chat_history.append({"role": "assistant", "content": reply})
    return jsonify({"response": reply})

